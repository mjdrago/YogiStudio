@model YogiStudio.Models.Bulletin

@{
    ViewBag.Title = "Bulletin";
}



<h2 id="mainHeader">Yogi Employee Chat</h2>
<hr />
<h3 id="quote">Try to be a rainbow in someone's cloud</h3>
<p>
    Welcome, Yogi instructors! Please use this bulletin to share thoughts, inspiration, and ask questions.
    This is a great spot to get advice from your fellow instructors. Feel free to plan get togethers, seminars
    or give other information that will help you all become even stronger Yoga masters.
</p>
<div id="container" class="col-md-12">
    @using (Html.BeginForm("ReceiveMessage", "Bulletin", FormMethod.Post))
    {
        <hr />
        <div class="row">
            @Html.TextBoxFor(model => model.Message, new { @class = "form-control", @placeholder = "New Post" })
            <button type="submit" class="btn btn-default">Submit</button>
        </div>
        <div>
            @Html.ValueFor(model => model.Message)
        </div>
            @*<div class="col-md-8">
                    @Html.TextBoxFor(model => model.Comment, new { @class = "form-control", @placeholder = "Add Comment" })
                    <hr />
                    <button type="submit" class="btn btn-default">Submit</button>
                </div>*@
    }
    <div>
        @{
            Html.RenderAction("_PartialBulletin", "Instructor");
        }
    </div>
</div>

@*@section scripts {
    <script src="~/Scripts/moment.js"></script>
    <script src="~/Scripts/jquery.signalR-2.0.3.min.js"></script>
    <script src="~/signalr/hubs"></script>
    <script src="~/Scripts/knockout-3.1.0.js"></script>
    <script src="~/Scripts/board.js"></script>*@
    }

@*<div class="row">
        <div class="col-md-12">
            <div data-bind="visible: notifications().length > 0, foreach: notifications">
                <div class="summary alert alert-success alert-dismissable">
                    <button type="button" class="close" data-dismiss="alert" aria-hidden="true"></button>
                    <p data-bind="text: $data"></p>
                </div>
            </div>

            <div class="new-post pad-bottom" data-bind="visible: signedIn">
                <form data-bind="submit: writePost">
                    <div class="form-group">
                        <label for="message">Write a new post:</label>
                        <textarea class="form-control" name="message" id="message" placeholder="New post"></textarea>
                    </div>
                    <button type="submit" class="btn btn-default">Submit</button>
                </form>
            </div>

            <ul class="posts list-unstyled" data-bind="foreach: posts">
                <li>
                    <p>
                        <span data-bind="text: username" class="username"></span><br />
                    </p>
                    <p>
                        <span data-bind="text: message"></span>
                    </p>

                    <p class="no-pad-bottom date-posted">Posted <span data-bind="text: moment(date).calendar()" /></p>

                    <div class="comments" data-bind="visible: $parent.signedIn() || comments().length > 0">
                        <ul class="list-unstyled" data-bind="foreach: comments, visible: comments().length > 0">
                            <li>

                                <p>
                                    <span class="commentor" data-bind="text: username"></span>
                                    <span data-bind="text: message"></span>
                                </p>
                                <p class="no-pad-bottom date-posted">Posted <span data-bind="text: moment(date).calendar()" /></p>
                            </li>
                        </ul>
                        <div class="new-post pad-bottom" data-bind="visible: signedIn">
                            <form class="add-comment" data-bind="visible: $parent.signedIn, submit: addComment">
                                <div class="form-group">
                                    <textarea class="form-control" name="message" id="message" placeholder="Add a comment"></textarea>
                                </div>
                                <button class="btn btn-default" type="submit">Add Comment</button>
                            </form>
                        </div>
                    </div>
                </li>
            </ul>
        </div>
    </div>*@








